package shared;

public interface Print {
	public void p (String title, String content);
	//private Print print  = (String title, String content) -> System.out.println("[" + title + "] : " + content);
	//print.p("main" , "Ω√¿€");
}
